import { describe, expect, it } from 'vitest';

import { BrowserScreen } from './browser-screen';

describe('BrowserScreen', () => {
  it('is defined', () => {
    expect(BrowserScreen).toBeDefined();
  });
});
