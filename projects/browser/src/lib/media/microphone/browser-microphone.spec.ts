import { describe, expect, it } from 'vitest';

import { BrowserMicrophone } from './browser-microphone';

describe('BrowserMicrophone', () => {
  it('is defined', () => {
    expect(BrowserMicrophone).toBeDefined();
  });
});
